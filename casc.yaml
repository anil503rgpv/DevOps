jenkins:
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: ${JENKINS_ADMIN_ID}
          password: ${JENKINS_ADMIN_PASSWORD}
unclassified:
  location:
    url: http://3.110.221.202:8081/
  plugin-catalog:
    plugins:
      - name: "ant"  # Replace with the actual plugin ID
        version: "latest"  # Replace with the desired plugin version
